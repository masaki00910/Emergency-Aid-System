steps:
- name: 'gcr.io/cloud-builders/docker'
  args: [
    'build',
    '-t', 'gcr.io/sharelabai-hackathon2/analyzer-agent:latest',
    '-f', 'agents/analyzer/Dockerfile',
    '.'
  ]
images: ['gcr.io/sharelabai-hackathon2/analyzer-agent:latest']